<!DOCTYPE html>
<html>
<head>
  <title><%= yield(:title) %></title>
  <%= stylesheet_link_tag :all %>
  <%= javascript_include_tag :defaults %>
  <%= javascript_include_tag 'rounded-corners-min' %>
  <%= csrf_meta_tag %>
</head>
<body>

<div class="header">
  <div id="user_navigation">
		<% if user_signed_in? %>
			Signed in as <%= current_user.email %>
			<%= link_to "Sugn out", destroy_user_session_path %>
		<% else %>
			<%= link_to "Sign up", new_user_registration_path %> or <%= link_to "Sign in", new_user_session_path %>
		<% end %>
	</div>

  <ul class="site_navigation group">
    <li><%= link_to t("site_navigation.needs"), needs_path, :class => "rc6" %></li>
  </ul>
</div>

<div class="main">
  <%= yield %>
</div>

<div class="footer">
  ©GavSav Group, <%= Date.today.year %>
</div>

</body>
</html>

