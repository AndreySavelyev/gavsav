<div style="background:#aaf;">
	<% if current_user.id == need_in_list.user_id %>
		<span style="float:right;">
			<%= link_to "Destroy", need_in_list, :method => :delete, :confirm => "Are you sure?" %>
		</span>
	<% end %>
	<h3><%= need_in_list.title %></h3>
	<div style="text-align:center;">
		<%= need_in_list.contents %>
	</div>

</div>